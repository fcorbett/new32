node_modules
.DS_Store
.figma/make/dev
.figma/make/env
.figma/make/install
.figma/make/setup
.figma/make/verify

# Local env (Vite)
.env
.env.local
.env.*.local

# Build output (CI builds and deploys; do not commit)
docs/

# Contact form secrets & runtime state (DreamHost)
public/api/config.php
public/api/storage/rate-limit.json
public/api/storage/*.json
!public/api/storage/.gitkeep
